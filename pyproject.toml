[project]
name = "mlops-course-students"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "pytest>=9.0.0",
    "pytest-cov>=7.0.0",
]
